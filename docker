FROM ghcr.io/cross-rs/aarch64-unknown-linux-musl:latest

RUN dpkg --add-architecture arm64 && \
    apt-get update && \
    apt-get --assume-yes install pkg-config && \
    apt-get --assume-yes install libssl-dev:arm64 && \
    apt-get --assume-yes install libudev-dev:arm64 libusb-1.0-0-dev
